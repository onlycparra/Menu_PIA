<Alloy>
	<Window class="container">
		
		<View id="header_view">
			<View id="back_view">
				<ImageView id="back_icon"/>
			</View>
			<View id="title_view">
				<Label id="title_label"/>
			</View>
			<View id="sound_view">
				<ImageView id="sound_icon"/>
			</View>
		</View>
		
		<View id="view_body">
			<ScrollView id="scrollView">
				<Button onClick="menu_siluetas">Siluetas</Button>
				<Button onClick="menu_encuentralos">Encuéntralos</Button>
				<Button onClick="menu_objetos">Objetos</Button>
				<Button onClick="menu_diferencias">Diferencias</Button>
				<Button onClick="menu_memorice">Memorice</Button>
			</ScrollView>
		</View>
		
    </Window>
</Alloy>